# Server Configuration
# Use 'development' for local development (HTTP only)
# Use 'production' for production environment (HTTPS with SSL)
NODE_ENV=development

# Port Configuration
PORT=80
HTTPS_PORT=443

# Domain Configuration
# Use 'localhost' for local development
# Use your actual domain in production
DOMAIN=localhost

# SSL Certificate Paths (only needed in production)
SSL_PRIVATE_KEY=/etc/letsencrypt/live/api.resumecheckers.com/privkey.pem
SSL_CERTIFICATE=/etc/letsencrypt/live/api.resumecheckers.com/fullchain.pem 